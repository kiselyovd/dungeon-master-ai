# Install torch separately with CUDA 12.8 wheel (NOT in this file because of
# platform-specific wheels: cu128 on Windows, CPU on macOS):
#   uv pip install --index-url https://download.pytorch.org/whl/cu128 torch==2.8.0 torchvision==0.23.0
# torch 2.8 minimum for nunchaku 1.2.x cp312 win_amd64 wheel.

# diffusers pinned to git main (~0.39.0.dev0) until 0.36 ships stable.
# Needed for ZImagePipeline + ZImageTransformer2DModel (Quality-OSS backend).
diffusers @ git+https://github.com/huggingface/diffusers.git@main

# transformers 4.51+ needed for Qwen3Model (Z-Image-Turbo text encoder).
# Capped < 5.0 because transformers 5.x removed FLAX_WEIGHTS_NAME and other
# symbols diffusers main still imports.
transformers>=4.51,<5.0

# peft 0.17+ required by diffusers 0.39 dep check.
peft>=0.17

# accelerate 1.x required by torch 2.8 (0.34 has incompatible meta-tensor handling).
accelerate>=1.0
safetensors>=0.4.5
sentencepiece==0.2.1
fastapi==0.137.1
uvicorn[standard]==0.47.0
pillow==10.4.0
pydantic==2.13.4
huggingface-hub>=0.27

# Video export deps (for LTX-Video MP4 generation via diffusers.utils.export_to_video):
opencv-python==4.13.0.92
imageio==2.37.3
imageio-ffmpeg==0.6.0

# nunchaku is intentionally NOT here - PyPI 'nunchaku' is an unrelated Bayesian sampler.
# Install nunchaku 1.2.1+ from nunchaku-tech/nunchaku GitHub release assets (wheel by
# torch+CUDA+python+OS triple). For RTX 3080 / Windows / Python 3.12:
#   gh release download v1.2.1 --repo nunchaku-tech/nunchaku \
#     --pattern "nunchaku-1.2.1+cu12.8torch2.8-cp312-cp312-win_amd64.whl"
#   uv pip install nunchaku-1.2.1+cu12.8torch2.8-cp312-cp312-win_amd64.whl
# nunchaku 1.1.0+ adds NunchakuZImageTransformer2DModel (used by Quality-OSS backend).
# einops auto-installed as transitive dep.
